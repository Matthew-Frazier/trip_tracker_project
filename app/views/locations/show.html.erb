<h4>Name: <%= @location.name %></h4>
<h4>Attraction: <%= @location.attraction %></h4>
<h4>Rating: <%= @location.rating %></h4>
<h4>Note: <%= @location.note%></h4>

<br />
<br />
<%= link_to 'Edit Location', edit_trip_location_path([@trip, @location]) %> |
<%= link_to 'Delete Location', [@trip, @location], method: :delete %> |
<%= link_to 'View Address', location_addresses_path(@location, @address)%> |
<%= link_to 'Back To Locations', trip_locations_path(@trip1, @location) %>
